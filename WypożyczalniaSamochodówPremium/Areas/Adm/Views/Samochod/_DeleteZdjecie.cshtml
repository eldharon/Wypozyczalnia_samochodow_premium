@using WypożyczalniaSamochodówPremium.Helpers
@using (Ajax.BeginForm("DeletePhoto", "Samochod",
    new
    {
        id = ViewBag.PhotoId
    },
    new AjaxOptions
    {
        HttpMethod = "POST",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "updatephoto",
        LoadingElementId = "autaBazaLoading",
        OnComplete = "",
        OnBegin = "HideModal",
        OnFailure = "",
        OnSuccess = ""
    }))


{

    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" onclick="HideModal()"><span aria-hidden="true"><i class="fa fa-times"></i></span><span class="sr-only">Close</span></button>
    </div>
    <div class="modal-body">

        <h4 class="modal-title">Czy na pewno chesz usunąć zdjęcie?</h4>

    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-white" data-dismiss="modal" onclick="HideModal()">Anuluj</button>
        <button type="submit" class="btn btn-primary btn-danger">Usuń</button>
    </div>
}
@*@using (Html.BeginForm("DeletePhoto", "Samochod", new { id = ViewBag.PhotoId }, FormMethod.Post, new { enctype = "multipart/form-data" }))*@